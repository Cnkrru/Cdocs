# 渲染循环

渲染循环（render loop）是 UI 库持续把 `Canvas` 缓冲搬到屏幕表面的过程。

## 两个阶段

- **逻辑更新**：处理输入、推进状态。
- **绘制**：调用 `canvas_set_pixel` 等图元刷新缓冲，再 blit 到窗口。

> 真实 App 的窗口由操作系统合成器保持显示，我们只需要每帧重绘内容即可。

## 与我们的关系

我们写的 `Canvas` 就是「要画的内容」，窗口后端负责把它贴到屏幕表面。
