# 构建产物（源码可重建，不入库）
dist/
web/dist/
.build/

# 生成器二进制（源码构建即可；bin/ 分发包的 exe 随仓库分发，保留）
Cdocs.exe
Cdocs
!bin/Cdocs.exe

# 分发包（web/.Cdocs/tools/make_release.py 可重建；bin/ 随仓库分发，不入忽略）
release/

# 日志与临时文件
*.log
*.tmp
*.swp
*~

# 本地工具记忆（个人开发记录，不入库）
.workbuddy/

# 系统文件
Thumbs.db
Desktop.ini
.DS_Store

# 部署测试遗留
_deploy_test_remote.git/
